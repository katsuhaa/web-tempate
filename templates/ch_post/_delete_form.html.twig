<form method="post" action="{{ path('app_ch_post_delete', {'id': ch_post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ch_post.id) }}">
    <button class="btn">Delete</button>
</form>
